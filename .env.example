# Anthropic: ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

LANGSMITH_TRACING=true
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=reviewguard

# Optional: Postgres connection string for the LangGraph checkpointer (e.g. Supabase's Session
# pooler URI). Falls back to a local SQLite file at data/graph_checkpoints.db when unset
DATABASE_URL=
